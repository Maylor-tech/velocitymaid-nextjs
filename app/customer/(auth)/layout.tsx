/**
 * Auth Layout - No authentication required
 * 
 * This layout wraps the login page and doesn't require authentication
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}


















