Metric tile for admin & operations dashboards.

```jsx
<KpiCard label="Jobs this week" value="128" delta={{ value: "12% vs last", direction: "up" }} icon={<Calendar size={20} />} />
```

Shows label, large Space Grotesk value, optional `subtitle`, optional `delta` trend (green up / red down), and an optional icon in a cyan-tint chip.
