Binary on/off toggle with a cyan track.

```jsx
<Switch label="Recurring weekly" defaultChecked />
<Switch checked={notify} onChange={setNotify} />
```
Use for settings/preferences; use `Checkbox` for form agreement and multi-select lists.
