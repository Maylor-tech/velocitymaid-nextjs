{
  "buildCommand": "rm -rf .next && SKIP_ENV_VALIDATION=true npm run build",
  "installCommand": "npm install --legacy-peer-deps --include=dev",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/weekly-payout-summary",
      "schedule": "0 11 * * 1"
    }
  ]
}